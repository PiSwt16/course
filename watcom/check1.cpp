#include <iostream>
#include <cstdlib>
#include <string>
#include <sstream>

using namespace std;

int main() {
    int a(10), b(20);
    cout << a - b << endl;
    cout << abs(a - b) << endl;
    

    return 0;
}
